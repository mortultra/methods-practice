// push method allows you to add one or more elements to an array and returns the new length of the array

const numbers = [1, 2, 3];

// just pushing numbers changes the array
numbers.push(4, 5, 6, 7);

// storing in a new variable and calling it returns just the length value
const total = numbers.push(4, 5, 6, 7);