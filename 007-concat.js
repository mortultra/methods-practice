// concat method allows you to merge two or more array into a new array. It does not change the original arrays, just creates a new copy. 

const a = [1, 2, 3];
const b = [4, 5, 6];
const d = [7, 8, 9];

const c = a.concat(b, d);


// const c = a.concat(1, 2, a)