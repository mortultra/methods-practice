// shift method removes the first element from the array and then returns it

const numbers = [1, 2, 3, 4]

// removes the first element in the array
numbers.shift()

// returns the elements that were removed
const firstItem = numbers.shift();